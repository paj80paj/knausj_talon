mode: dictation
mode: command

-

(drowsey) [<phrase>]$: 
    mode.disable("command")
    mode.disable("dictation")
    mode.enable("sleep")
   
# key(keypad_0): 
#     mode.disable("command")
#     mode.disable("dictation")
#     mode.enable("sleep")
